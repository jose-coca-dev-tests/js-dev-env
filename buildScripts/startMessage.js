// this is a library for adding coloured messages to the output console
import chalk from "chalk";
// the comment at the end of the line will disable the console rule of eslint for this line only
console.log(chalk.green("Starting app in dev mode...")); // eslint-disable-line no-console
